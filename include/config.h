#ifndef CONFIG_H
#define CONFIG_H

#define WIFI_SSID           "XXXXXXXXXXXXXXXX"
#define WIFI_PWD            "XXXXXXXXXXX"

#define WEB_HOST            "XXXXXXXXXXX.com"

#define BOARD_NAME          "HOME_EXTERNAL_1" //this is the name of the board that will be saved in your database on every log collected so you can filter by the place you're using this board at. Default is HOME_EXTERNAL_1 because normally people tend to have only one board, located outside (garden or so) on their home
#endif // CONFIG_H